spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Turkcell-s-7th-Team/Library-microservices
          default-label: main
          search-paths:
            - config-server
            - configurations/
            - configurations/{application}

server:
  port: 8787
