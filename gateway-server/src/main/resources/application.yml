spring:
  application:
    name: gateway-server
  profiles:
    active: dev
  config:
    import: configserver:${CONFIG_URI:http://localhost:8787}

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
